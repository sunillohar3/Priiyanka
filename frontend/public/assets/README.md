# Frontend asset placeholders

Drop the shared image files into this folder:
- `logo.png`
- `profile.jpg`

The application refers to these assets as:
- `/assets/logo.png` for the navbar logo
- `/assets/profile.jpg` for the profile image on Home and About pages
